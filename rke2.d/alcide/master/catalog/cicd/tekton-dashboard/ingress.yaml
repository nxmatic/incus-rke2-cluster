apiVersion: networking.k8s.io/v1
kind: Ingress
metadata: # kpt-merge: tekton-pipelines/tekton-dashboard
  name: tekton-dashboard
  namespace: tekton-pipelines
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: web
    internal.kpt.dev/upstream-identifier: 'networking.k8s.io|Ingress|tekton-pipelines|tekton-dashboard'
    kube-vip.io/address-pool: lan-lb
    traefik.ingress.kubernetes.io/router.middlewares: tekton-dashboard-strip@kubernetescrd
spec:
  ingressClassName: traefik
  rules:
    - host: ${cluster-name}-web-proxy.lan # kpt-set: ${cluster-name}-web-proxy.lan
      http:
        paths:
          - path: /tekton
            pathType: Prefix
            backend:
              service:
                name: tekton-dashboard
                port:
                  number: 9097
