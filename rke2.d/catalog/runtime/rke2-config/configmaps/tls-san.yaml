apiVersion: v1
kind: ConfigMap
metadata: # kpt-merge: /rke2-tls-san
  name: tls-san.yaml
  annotations:
    description.kpt.dev: TLS SAN fragment
    config.kubernetes.io/local-config: "true"
data:
  tls-san:
    - localhost
    - gateway
    - 0.0.0.0
    - 127.0.0.1
    - ${cluster-vip-gateway-inetaddr} # kpt-set: ${cluster-vip-gateway-inetaddr}
    - ${node-gateway-inetaddr} # kpt-set: ${node-gateway-inetaddr}
    - ${node-host-inetaddr} # kpt-set: ${node-host-inetaddr}
