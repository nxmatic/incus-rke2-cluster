apiVersion: v1
kind: ConfigMap
metadata:
  name: set-namespace-config
  annotations:
    config.kubernetes.io/local-config: "true"
    internal.kpt.dev/function-config: set-namespace
data:
  namespace: tekton-pipelines # kpt-set: ${target-namespace}
