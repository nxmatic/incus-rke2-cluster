# Full profile overlay: adds Cilium full-feature HelmChartConfig + cluster resources
# @codebase

# All rendered resources now live inside the fleet subtree and are
# materialized on each control-plane node via rke2-manifests-install.
# No extra systemd glue is required in this overlay.

write_files: []
runcmd: []

# Cilium advanced features (BGP, L2, IP pools) remain authored in the fleet repo.
# See: ${CLUSTER_STATE_DIR}/${CLUSTER_NAME}/networking/cilium

# Envoy Gateway remains managed via the same static manifest pipeline.
# See: ${CLUSTER_STATE_DIR}/${CLUSTER_NAME}/networking/envoy-gateway