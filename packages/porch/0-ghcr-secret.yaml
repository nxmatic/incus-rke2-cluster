apiVersion: v1
kind: Secret
metadata: # kpt-merge: porch-system/ghcr-pull
  name: ghcr-pull # kpt-set: ${porch-image-pull-secret}
  namespace: porch-system
  annotations:
    internal.kpt.dev/upstream-identifier: '|Secret|porch-system|ghcr-pull'
type: kubernetes.io/dockerconfigjson
stringData:
  .dockerconfigjson: "" # kpt-set: ${porch-docker-config-json}
