apiVersion: v1
kind: ConfigMap
metadata:
  name: rke2-core
  annotations:
    config.kubernetes.io/local-config: "true"
    description.kpt.dev: Core RKE2 settings
data:
  core.yaml: |
    write-kubeconfig-mode: "0640"
    bind-address: "0.0.0.0"
    cni:
      - cilium
    ingress-controller: traefik
